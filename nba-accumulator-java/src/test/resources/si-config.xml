<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-file="http://www.springframework.org/schema/integration/file"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
	http://www.springframework.org/schema/integration/file http://www.springframework.org/schema/integration/file/spring-integration-file.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<context:component-scan base-package="com.rossotti.basketball.app.gateway" />

	<int-file:inbound-channel-adapter id="producer-file-adapter" channel="inboundChannel" directory="classpath:/" filename-pattern="si-properties.json" prevent-duplicates="true">
		<int:poller fixed-rate="5000" />
	</int-file:inbound-channel-adapter>

	<int:channel id="inboundChannel" />

	<int:json-to-object-transformer id="incomingJsonConverter" input-channel="inboundChannel" output-channel="findGameInChannel" type="com.rossotti.basketball.app.gateway.ServiceProperties" />

	<int:channel id="findGameInChannel" />

	<int:service-activator ref="findGameBean" input-channel="findGameInChannel" output-channel="findGameOutChannel" method="findGames" />
	<bean id="findGameBean" class="com.rossotti.basketball.app.gateway.FindGameActivator" />

	<int:channel id="findGameOutChannel">
		<int:queue capacity="20" />
	</int:channel>

	<int:splitter ref="gameSplitter" input-channel="findGameOutChannel" output-channel="splitGameChannel" />
	<bean id="gameSplitter" class="com.rossotti.basketball.app.gateway.GameSplitter" />

	<int:channel id="splitGameChannel" />

	<int:router ref="gameRouter" input-channel="splitGameChannel"
		method="routeGame" />
	<bean id="gameRouter" class="com.rossotti.basketball.app.gateway.GameRouter" />

	<int:channel id="scheduledChannelRoute" />
	<int:channel id="completedChannelRoute" />

	<int:object-to-json-transformer id="outgoingJsonConverter1" input-channel="scheduledChannelRoute" output-channel="scheduledChannelOut" />
	<int:object-to-json-transformer id="outgoingJsonConverter2" input-channel="completedChannelRoute" output-channel="completedChannelOut" />

	<int:channel id="scheduledChannelOut" />
	<int:channel id="completedChannelOut" />

	<int-file:outbound-channel-adapter channel="scheduledChannelOut" id="consumer-file-adapter1" directory="file:////home/pablote/pdrive/pwork/rest/nba-accumulator/message/scheduled" />
	<int-file:outbound-channel-adapter channel="completedChannelOut" id="consumer-file-adapter2" directory="file:////home/pablote/pdrive/pwork/rest/nba-accumulator/message/completed" />

	<int:poller id="defaultPoller" default="true" max-messages-per-poll="1" fixed-rate="200" />

</beans>